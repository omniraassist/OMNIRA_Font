# Copy each line into Vercel → Backend project → Settings → Environment Variables → Production.
# Replace PLACEHOLDER values. Never commit real secrets to git.

# --- Your two secrets (from you) ---
# 1) Stripe Dashboard → Developers → API keys → Secret key (sk_live_…)
STRIPE_SECRET_KEY=PASTE_sk_live_HERE

# 2) Same random string you use for customer JWT (must match what you used when testing locally)
CUSTOMER_JWT_SECRET=PASTE_LONG_RANDOM_SECRET_HERE

# --- Third secret (webhook — not the same as sk_live) ---
# Stripe Dashboard → Developers → Webhooks → Add endpoint:
#   URL: https://YOUR-BACKEND.vercel.app/api/stripe/webhook
#   Events: checkout.session.completed, payment_intent.succeeded
# Then "Reveal" signing secret → whsec_…
STRIPE_WEBHOOK_SECRET=PASTE_whsec_FROM_DASHBOARD_HERE

# Publishable key (pk_live_…) — embedded Stripe Elements in customer panel
STRIPE_PUBLISHABLE_KEY=PASTE_pk_live_HERE

# --- Public URL of the deployed frontend (no trailing slash) ---
# Stripe redirects here after pay: /?checkout=success&session_id=…
PUBLIC_APP_URL=https://YOUR-FRONTEND.vercel.app

# --- Supabase (same as local .env) ---
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# Use the JWT "service_role" key from Supabase → Settings → API (legacy: service_role secret).
SUPABASE_SERVICE_ROLE_KEY=PASTE_service_role_jwt_HERE
# Optional: if you only set the new sb_secret key name, the server also reads:
# SUPABASE_SECRET_KEY=
SUPABASE_ANON_KEY=PASTE_anon_jwt_HERE
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
SUPABASE_PROJECT_ID=your_project_ref

# --- Meta WhatsApp (Cloud API webhook on THIS backend project only) ---
# Callback URL in Meta (exact path): https://YOUR-BACKEND.vercel.app/api/meta/whatsapp/webhook
# Copy the same values you use in server/.env locally — they are NOT deployed from .git.
META_WABA_VERIFY_TOKEN=PASTE_same_random_string_as_in_Meta_webhook_verify_field
META_WABA_ACCESS_TOKEN=PASTE_long_lived_system_user_token_with_whatsapp_business_messaging
META_WABA_PHONE_NUMBER_ID=PASTE_phone_number_id_from_Meta_WhatsApp_API_setup
META_WABA_GRAPH_VERSION=v21.0
# App secret for X-Hub-Signature-256 (Meta → App → Settings → Basic). Prefer this over skipping signature.
META_WABA_APP_SECRET=PASTE_app_secret_here
# Only if you cannot set META_WABA_APP_SECRET yet: true (Meta POSTs otherwise return 403 on Vercel).
META_WABA_WEBHOOK_SKIP_SIGNATURE=false

# OpenAI for auto-replies (unless you only use META_WABA_MARKETING_AUTO_REPLY fixed text)
OPENAI_API_KEY=PASTE_sk_openai_here
# OPENAI_CHAT_MODEL=gpt-4o-mini

# Optional overrides
# META_WABA_OPENAI_SYSTEM=
# META_WABA_OPENAI_FALLBACK_REPLY=
# META_WABA_MARKETING_AUTO_REPLY=

# --- Do NOT set these in production ---
# STRIPE_WEBHOOK_INSECURE_LOCAL=
# META_WABA_WEBHOOK_INSECURE_LOCAL=
# OMNIRA_ALLOW_SUBSCRIPTION_SIMULATE=
